/* pages/project-builder/project-builder.wxss */
page {
  overflow-x: hidden;
  background: radial-gradient(circle at 90% 10%, rgba(109, 40, 217, 0.08), transparent 24%), linear-gradient(180deg, #fbfdff 0%, #f7f9ff 54%, #ffffff 100%);
  font-family: -apple-system, 'PingFang SC', sans-serif;
  -webkit-font-smoothing: antialiased;
}

/* 閳烘劏鏅?Hero 閳烘劏鏅?*/
.hero {
  box-shadow: 0 22rpx 44rpx rgba(109, 40, 217, 0.24);
  overflow: hidden;
  border-radius: 34rpx;
  margin: 16rpx 24rpx 28rpx;
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  background: linear-gradient(135deg, #27115f 0%, #6d28d9 56%, #2f66f4 100%);
  padding: 44rpx 34rpx 42rpx;
  text-align: center;
}
.hero-icon  { font-size: 60rpx; margin-bottom: 14rpx; }
.hero-title { font-size: 36rpx; font-weight: 700; color: #fff; margin-bottom: 10rpx; letter-spacing: 1rpx; }
.hero-sub   { font-size: 25rpx; color: rgba(255,255,255,0.82); line-height: 1.7; }

/* 閳烘劏鏅?妫板嫬锝為幓鎰仛 閳烘劏鏅?*/
.prefill-tip {
  margin: 16rpx 24rpx 0;
  padding: 16rpx 22rpx;
  background: rgba(94,106,210,0.08);
  border: 1rpx solid rgba(94,106,210,0.18);
  border-radius: var(--r-md);
  font-size: 25rpx;
  color: var(--brand);
  line-height: 1.5;
}

/* 閳烘劏鏅?鐞涖劌宕熼崠鍝勬健 閳烘劏鏅?*/
.form-section {
  box-shadow: 0 16rpx 36rpx rgba(38, 27, 75, 0.08);
  background: linear-gradient(180deg, #ffffff 0%, #fbfdff 100%);
  border-radius: 32rpx;
  border: 1rpx solid rgba(71, 42, 132, 0.08);
  padding: 28rpx 24rpx 30rpx;
  margin: 24rpx 24rpx 0;
}

.section-label {
  line-height: 42rpx;
  display: flex;
  align-items: center;
  font-size: 29rpx;
  font-weight: 850;
  color: #172033;
  margin-bottom: 22rpx;
}
.section-label.no-margin { margin-bottom: 0; }
.section-label-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 22rpx;
}
.label-num {
  box-shadow: 0 10rpx 20rpx rgba(47, 102, 244, 0.2);
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 44rpx; height: 44rpx;
  background: linear-gradient(135deg, #2f66f4 0%, #7c3aed 100%);
  color: #fff;
  border-radius: 15rpx;
  font-size: 23rpx;
  font-weight: 900;
  margin-right: 12rpx;
  flex-shrink: 0;
}
.required { color: var(--danger); font-weight: 500; }
.optional  { color: var(--text-muted); font-size: 23rpx; font-weight: 400; margin-left: 4rpx; }
.char-count { font-size: 22rpx; color: var(--text-muted); }
.field-hint { font-size: 23rpx; color: var(--text-muted); line-height: 1.5; margin-bottom: 12rpx; display: block; }

/* 閳烘劏鏅?閺傜懓鎮滈崡锛勫缂冩垶鐗?閳烘劏鏅?*/
.track-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 18rpx;
}
.track-card {
  transition: transform 0.16s ease, opacity 0.16s ease;
  min-height: 134rpx;
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 24rpx 10rpx 22rpx;
  background: #fbf8ff;
  border-radius: 26rpx;
  border: 1rpx solid rgba(109, 40, 217, 0.1);
  text-align: center;
  overflow: hidden;
}
.track-card:active { opacity: 0.75; }
.track-card.track-selected { border-width: 2.5rpx; }

.track-icon  { font-size: 40rpx; margin-bottom: 8rpx; line-height: 1; }
.track-label { font-size: 26rpx; font-weight: 700; color: var(--text-primary); margin-bottom: 4rpx; }
.track-desc  { font-size: 19rpx; color: var(--text-muted); line-height: 1.4; }
.track-check {
  position: absolute;
  top: 10rpx; right: 10rpx;
  width: 28rpx; height: 28rpx;
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  font-size: 16rpx; font-weight: 800; color: #fff;
}

/* 閳烘劏鏅?缁狙冨焼 Chips 閳烘劏鏅?*/
.chip-row { display: flex; gap: 14rpx; }
.chip {
  transition: transform 0.16s ease, opacity 0.16s ease;
  border-radius: 999rpx;
  font-weight: 750;
  color: #566174;
  justify-content: center;
  align-items: center;
  display: flex;
  min-height: 72rpx;
  flex: 1; text-align: center;
  font-size: 27rpx;
  padding: 0 22rpx;
  background: #ffffff;
  border: 1rpx solid rgba(23, 32, 51, 0.08);
}
.chip.chip-active {
  box-shadow: 0 10rpx 22rpx rgba(109, 40, 217, 0.2);
  background: linear-gradient(135deg, #6d28d9, #2f66f4);
  color: #ffffff;
  border-color: transparent;
}

/* 閳烘劏鏅?閺傚洦婀版潏鎾冲弳 閳烘劏鏅?*/
.text-input {
  height: 90rpx;
  width: 100%; height: 84rpx;
  background: #faf7ff;
  border-radius: 22rpx;
  padding: 0 26rpx;
  font-size: 28rpx;
  border: 1rpx solid rgba(109, 40, 217, 0.12);
  box-sizing: border-box;
}
.text-area {
  min-height: 176rpx;
  width: 100%; min-height: 140rpx;
  background: #faf7ff;
  border-radius: 22rpx;
  padding: 24rpx 26rpx;
  font-size: 28rpx;
  border: 1rpx solid rgba(109, 40, 217, 0.12);
  box-sizing: border-box;
  line-height: 1.55;
}

/* 閳烘劏鏅?閻㈢喐鍨氶幐澶愭尦 閳烘劏鏅?*/
.gen-btn {
  box-shadow: 0 14rpx 28rpx rgba(109, 40, 217, 0.22);
  border-radius: 999rpx;
  --btn-font-size: 28rpx;
  --btn-height: 82rpx;
  display: block;
  max-width: calc(100vw - 136rpx);
  margin: 26rpx auto 18rpx;
  width: 560rpx;
}

/* 閳烘劏鏅?閸樺棗褰剁拋鏉跨秿 閳烘劏鏅?*/
.history-section {
  box-shadow: 0 16rpx 36rpx rgba(38, 27, 75, 0.08);
  padding: 28rpx 24rpx;
  margin: 28rpx 24rpx 0;
  background: linear-gradient(180deg, #ffffff 0%, #fbfdff 100%);
  border-radius: 30rpx;
  border: 1rpx solid rgba(71, 42, 132, 0.08);
  overflow: hidden;
}
.history-head {
  display: flex; align-items: center; justify-content: space-between;
  padding: 22rpx 28rpx;
}
.history-title  { font-size: 27rpx; font-weight: 600; color: var(--text-primary); }
.history-toggle { font-size: 22rpx; color: var(--text-muted); }
.history-list   { border-top: 1rpx solid #F3F4F6; }
.history-card {
  transition: transform 0.16s ease, opacity 0.16s ease;
  border-radius: 26rpx;
  display: flex; align-items: center;
  padding: 24rpx;
  border-bottom: 1rpx solid #F3F4F6;
}
.history-card:active { background: rgba(0,0,0,0.03); }
.hc-icon-wrap {
  width: 64rpx; height: 64rpx; border-radius: 14rpx;
  background: #F3F4F6;
  display: flex; align-items: center; justify-content: center;
  flex-shrink: 0;
}
.hc-icon  { font-size: 32rpx; }
.hc-info  { flex: 1; min-width: 0; }
.hc-title {
  display: block; font-size: 27rpx; font-weight: 600; color: var(--text-primary);
  overflow: hidden; text-overflow: ellipsis; white-space: nowrap; margin-bottom: 4rpx;
}
.hc-meta  { font-size: 22rpx; color: var(--text-muted); }
.hc-arrow { font-size: 34rpx; color: var(--text-muted); flex-shrink: 0; }
.history-clear-btn {
  display: flex; justify-content: center; padding: 18rpx;
}
.history-clear-txt { font-size: 24rpx; color: var(--danger); }

/* 閳烘劏鏅?閸旂姾娴囨い?閳烘劏鏅?*/
.loading-page {
  background: radial-gradient(circle at center 28%, rgba(109, 40, 217, 0.12), transparent 38%), linear-gradient(180deg, #faf7ff, #ffffff);
  display: flex; flex-direction: column;
  align-items: center; justify-content: center;
  min-height: 80vh; padding: 0 48rpx; gap: 32rpx;
}
.loading-anim {
  position: relative; width: 120rpx; height: 120rpx;
  display: flex; align-items: center; justify-content: center;
}
.pulse-ring {
  position: absolute; width: 120rpx; height: 120rpx;
  border-radius: 50%; border: 4rpx solid #6D28D9;
  animation: pulse 1.4s ease-in-out infinite;
}
.pulse-core {
  width: 80rpx; height: 80rpx; border-radius: 50%;
  background: linear-gradient(135deg, var(--brand), #6D28D9);
  color: #fff; font-size: 28rpx; font-weight: 800;
  display: flex; align-items: center; justify-content: center;
}
@keyframes pulse {
  0%   { transform: scale(0.9); opacity: 1; }
  70%  { transform: scale(1.25); opacity: 0.2; }
  100% { transform: scale(0.9); opacity: 1; }
}
.loading-tip {
  font-size: 30rpx; font-weight: 600; color: var(--text-primary); text-align: center;
}
.loading-steps {
  display: flex; align-items: center; gap: 10rpx;
}
.lstep {
  font-size: 22rpx; color: var(--text-muted);
  padding: 7rpx 18rpx; border-radius: var(--r-lg); background: #F3F4F6;
}
.lstep.done { color: #6D28D9; background: #F5F3FF; font-weight: 600; }
.lstep-sep  { font-size: 26rpx; color: #D1D5DB; }
.loading-note { font-size: 24rpx; color: var(--text-muted); }

/* 閳烘劏鏅?缂佹挻鐏夋い?閳烘劏鏅?*/
.result-page { padding: 16rpx 24rpx; }

.result-header {
  background: var(--bg-panel);
  border-radius: var(--r-lg);
  padding: 28rpx;
  margin-bottom: 16rpx;
  border: 1.5rpx solid rgba(0,0,0,0.05);
  box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.03);
}
.result-title-row {
  display: flex; align-items: flex-start;
  justify-content: space-between; gap: 16rpx;
  margin-bottom: 16rpx;
}
.result-project-title {
  font-size: 34rpx; font-weight: 800; color: var(--text-primary);
  flex: 1; line-height: 1.35;
}
.result-duration {
  flex-shrink: 0;
  background: #F5F3FF;
  border-radius: 8rpx;
  padding: 6rpx 16rpx;
  margin-top: 4rpx;
}
.duration-txt { font-size: 22rpx; color: #6D28D9; font-weight: 600; }

.result-track-row {
  display: flex; align-items: center; justify-content: space-between;
}
.result-track-badge {
  display: flex; align-items: center; gap: 8rpx;
}
.result-track-icon  { font-size: 28rpx; }
.result-track-label { font-size: 24rpx; font-weight: 600; }
.top-btn {
  display: flex; align-items: center; gap: 6rpx;
  padding: 10rpx 22rpx; border-radius: 9999rpx;
  background: rgba(0,0,0,0.04); border: 1rpx solid rgba(0,0,0,0.08);
}
.top-btn-icon { font-size: 26rpx; color: var(--text-muted); }
.top-btn-txt  { font-size: 24rpx; color: var(--text-muted); font-weight: 500; }

/* 閳烘劏鏅?缂佹挻鐏夐崡锛勫 閳烘劏鏅?*/
.result-card {
  transition: transform 0.16s ease, opacity 0.16s ease;
  overflow: hidden;
  margin-top: 22rpx;
  background: linear-gradient(180deg, #ffffff 0%, #fbfdff 100%);
  border-radius: 32rpx;
  padding: 30rpx 28rpx;
  margin-bottom: 24rpx;
  border: 1rpx solid rgba(71, 42, 132, 0.08);
  box-shadow: 0 16rpx 36rpx rgba(38, 27, 75, 0.08);
}

.card-label-row {
  display: flex; align-items: center; gap: 10rpx;
  margin-bottom: 16rpx;
}
.card-label-icon { font-size: 28rpx; }
.card-label-txt  { font-size: 27rpx; font-weight: 700; color: var(--text-primary); flex: 1; }
.card-label-sub  { font-size: 22rpx; color: var(--text-muted); }

.card-body-text {
  font-size: 27rpx; color: var(--text-body);
  line-height: 1.8; display: block;
}

/* 閺佺増宓侀弶銉︾爱 + 閹垛偓閺堫垱鐖ら獮鑸靛笓 */
.two-col-card { display: flex; gap: 0; }
.half-col     { flex: 1; min-width: 0; }
.col-divider  { width: 1.5rpx; background: #F3F4F6; margin: 0 20rpx; flex-shrink: 0; }

.tag-list { display: flex; flex-wrap: wrap; gap: 10rpx; }
.data-tag {
  font-size: 22rpx; color: #3B82F6;
  background: #EFF6FF; border: 1rpx solid #BFDBFE;
  border-radius: 8rpx; padding: 5rpx 14rpx;
}
.tech-tag {
  font-size: 22rpx; color: #059669;
  background: #ECFDF5; border: 1rpx solid #A7F3D0;
  border-radius: 8rpx; padding: 5rpx 14rpx;
}

/* 閸忔娊鏁幋鎰亯 */
.results-list { display: flex; flex-direction: column; gap: 14rpx; }
.result-item  { display: flex; align-items: flex-start; gap: 14rpx; }
.result-num-badge {
  width: 36rpx; height: 36rpx; border-radius: 50%;
  background: linear-gradient(135deg, var(--brand), #6D28D9);
  color: #fff; font-size: 20rpx; font-weight: 800;
  display: flex; align-items: center; justify-content: center;
  flex-shrink: 0; margin-top: 2rpx;
}
.result-item-txt {
  font-size: 27rpx; color: var(--text-body); line-height: 1.7; flex: 1;
}

/* 缁犫偓閸樺棔绔寸悰宀€澧?*/
.resume-bullet-card { cursor: pointer; }
.resume-bullet-card:active { opacity: 0.75; }
.bullet-copy-hint {
  font-size: 22rpx; color: var(--brand);
  background: #EEF2FF; padding: 4rpx 14rpx;
  border-radius: 8rpx; flex-shrink: 0;
}
.resume-bullet-text {
  font-size: 27rpx; color: var(--brand);
  line-height: 1.7; display: block;
  background: #EEF2FF;
  border-radius: var(--r-sm);
  padding: 16rpx 18rpx;
  border-left: 5rpx solid var(--brand);
}

/* 閳烘劏鏅?閹垮秳缍旈幐澶愭尦缂?閳烘劏鏅?*/
.action-btn {
  flex: 1; display: flex;
  align-items: center; justify-content: center;
  gap: 10rpx; height: 88rpx;
  border-radius: 44rpx;
  font-size: 28rpx; font-weight: 600;
}
.action-btn:active { transform: scale(0.97); opacity: 0.85; }

.action-copy {
  background: var(--bg-panel);
  border: 1.5rpx solid rgba(0,0,0,0.08);
  color: var(--text-secondary);
}
.action-btn-icon { font-size: 30rpx; }
.action-btn-txt  { font-size: 28rpx; }

/* 2026 kingkong polish: project builder */
.hero::after {
  content: '';
  position: absolute;
  width: 260rpx;
  height: 260rpx;
  right: -96rpx;
  top: -96rpx;
  border-radius: 999rpx;
  background: rgba(255, 255, 255, 0.16);
}

.hero-title {
  color: #ffffff;
  font-size: 42rpx;
  letter-spacing: -0.8rpx;
}

.action-group {
  margin-left: 24rpx;
  margin-right: 24rpx;
}

.action-resume {
  background: linear-gradient(135deg, #6d28d9, #2f66f4);
  box-shadow: 0 14rpx 28rpx rgba(109, 40, 217, 0.22);
}

/* 2026 project builder spacing polish */
.hero-icon {
  width: 72rpx;
  height: 72rpx;
  margin: 0 auto 18rpx;
  border-radius: 24rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(255, 255, 255, 0.14);
  box-shadow: inset 0 0 0 1rpx rgba(255, 255, 255, 0.18);
}

.hero-sub {
  margin-top: 12rpx;
  line-height: 1.6;
}

.form-section + .form-section {
  margin-top: 26rpx;
}

/* 2026 project builder bottom tighten */

/* Visual stability restore: keep final project builder overrides last. */
page {
  overflow-x: hidden;
  background:
    radial-gradient(circle at 14% -5%, rgba(109, 40, 217, 0.12), transparent 34%),
    radial-gradient(circle at 92% 10%, rgba(37, 99, 235, 0.10), transparent 30%),
    #f6f8fc;
}

.page {
  overflow-x: hidden;
  padding-bottom: 0;
}

.hero {
  position: relative;
  margin: 16rpx 24rpx 18rpx;
  padding: 36rpx 30rpx;
  overflow: hidden;
  border-radius: 34rpx;
  background:
    radial-gradient(circle at 86% 12%, rgba(255,255,255,0.24), transparent 32%),
    linear-gradient(135deg, #3b0764 0%, #6d28d9 50%, #2563eb 130%);
  box-shadow: 0 22rpx 48rpx rgba(109, 40, 217, 0.20);
}

.hero-icon {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 72rpx;
  height: 72rpx;
  margin: 0 auto 18rpx;
  border-radius: 24rpx;
  background: rgba(255,255,255,0.14);
  box-shadow: inset 0 0 0 1rpx rgba(255,255,255,0.18);
}

.hero-title {
  color: #ffffff;
  font-size: 42rpx;
  font-weight: 950;
  letter-spacing: -0.8rpx;
}

.hero-sub {
  margin-top: 12rpx;
  color: rgba(255,255,255,0.78);
  font-size: 25rpx;
  line-height: 1.6;
}

.form-section,
.history-section,
.result-header,
.result-card {
  margin-left: 24rpx;
  margin-right: 24rpx;
  border-radius: 30rpx;
  background: linear-gradient(180deg, #ffffff, #fbfdff);
  border: 1rpx solid rgba(109, 40, 217, 0.08);
  box-shadow: 0 14rpx 34rpx rgba(15, 23, 42, 0.06);
}

.form-section + .form-section {
  margin-top: 26rpx;
}

.section-label,
.section-label-row {
  color: #172033;
  font-weight: 850;
}

.label-num {
  color: #6d28d9;
  background: #f4efff;
}

.track-card,
.chip,
.text-input,
.text-area {
  background: #f8fafc;
  border: 1rpx solid #e6ebf2;
}

.track-card.track-selected {
  color: #101828;
  background: linear-gradient(180deg, #ffffff 0%, #f8f5ff 100%);
  border-color: #8b5cf6;
  box-shadow: 0 12rpx 26rpx rgba(109, 40, 217, 0.14);
}

.track-card.track-selected .track-label {
  color: #6d28d9 !important;
}

.track-card.track-selected .track-desc {
  color: #475467;
}

.track-label {
  line-height: 1.2;
}

.track-desc {
  line-height: 1.35;
  word-break: keep-all;
}

.chip.chip-active {
  color: #ffffff;
  background: linear-gradient(135deg, #6d28d9, #2563eb);
  border-color: transparent;
  box-shadow: 0 12rpx 24rpx rgba(109, 40, 217, 0.18);
}

.text-input,
.text-area {
  border-radius: 22rpx;
  color: #172033;
}

.gen-btn {
  margin-top: 24rpx;
  border-radius: 24rpx;
  background: linear-gradient(135deg, #6d28d9, #2563eb);
  box-shadow: 0 16rpx 30rpx rgba(109, 40, 217, 0.22);
}

.action-group {
  margin-left: 24rpx;
  margin-right: 24rpx;
}

.action-resume {
  background: linear-gradient(135deg, #6d28d9, #2f66f4);
  box-shadow: 0 14rpx 28rpx rgba(109, 40, 217, 0.22);
}

.loading-page {
  background:
    radial-gradient(circle at 50% 8%, rgba(109, 40, 217, 0.12), transparent 36%),
    #f6f8fc;
}
/* Unified homepage background */
page,
.page,
.container {
  --app-page-bg: radial-gradient(circle at 18% 0%, rgba(43, 92, 230, 0.08) 0%, rgba(43, 92, 230, 0) 34%), linear-gradient(180deg, #f8fbff 0%, #ffffff 42%);
  background: var(--app-page-bg);
  background-color: #ffffff;
}

/* Premium loading state */
.loading-page {
  min-height: 82vh;
  padding: 48rpx 32rpx 80rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  box-sizing: border-box;
  background:
    linear-gradient(180deg, #faf9ff 0%, #f7f8fc 56%, #ffffff 100%);
}

.loading-card {
  width: 100%;
  max-width: 650rpx;
  padding: 46rpx 34rpx 36rpx;
  border-radius: 34rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  background:
    linear-gradient(180deg, rgba(255,255,255,0.96), rgba(255,255,255,0.98)),
    #ffffff;
  border: 1rpx solid rgba(109, 40, 217, 0.1);
  box-shadow: 0 24rpx 70rpx rgba(16, 24, 40, 0.09);
  box-sizing: border-box;
}

.loading-mark {
  position: relative;
  width: 132rpx;
  height: 132rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.mark-ring {
  position: absolute;
  inset: 0;
  border-radius: 50%;
  border: 6rpx solid rgba(109, 40, 217, 0.16);
  border-top-color: #6d28d9;
  border-right-color: #2563eb;
  animation: loadingSpin 1.2s linear infinite;
}

.mark-core {
  width: 86rpx;
  height: 86rpx;
  border-radius: 28rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #ffffff;
  background: linear-gradient(135deg, #6d28d9, #2563eb);
  box-shadow: 0 14rpx 28rpx rgba(109, 40, 217, 0.22);
  font-size: 27rpx;
  font-weight: 950;
}

@keyframes loadingSpin {
  to { transform: rotate(360deg); }
}

.loading-eyebrow {
  margin-top: 30rpx;
  color: #6d28d9;
  font-size: 21rpx;
  font-weight: 950;
  letter-spacing: 0;
}

.loading-tip {
  margin-top: 12rpx;
  color: #101828;
  font-size: 34rpx;
  line-height: 1.32;
  font-weight: 950;
  text-align: center;
}

.loading-desc {
  margin-top: 14rpx;
  max-width: 540rpx;
  color: #667085;
  font-size: 25rpx;
  line-height: 1.65;
  text-align: center;
}

.loading-progress {
  width: 100%;
  height: 12rpx;
  margin-top: 30rpx;
  border-radius: 999rpx;
  overflow: hidden;
  background: #eef2f7;
}

.loading-progress-fill {
  height: 100%;
  border-radius: 999rpx;
  background: linear-gradient(90deg, #6d28d9, #2563eb);
  transition: width 0.35s ease;
}

.loading-steps {
  width: 100%;
  margin-top: 26rpx;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 12rpx;
}

.lstep {
  min-height: 100rpx;
  padding: 16rpx 12rpx;
  border-radius: 22rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  color: #98a2b3;
  background: #f8fafc;
  border: 1rpx solid rgba(230, 235, 242, 0.95);
  box-sizing: border-box;
}

.lstep.done {
  color: #5b21b6;
  background: #f5f3ff;
  border-color: rgba(109, 40, 217, 0.16);
  font-weight: 900;
}

.lstep-index {
  font-size: 20rpx;
  line-height: 1.2;
  font-weight: 950;
}

.lstep-title {
  margin-top: 8rpx;
  font-size: 24rpx;
  line-height: 1.2;
  font-weight: 900;
}

.loading-note {
  margin-top: 24rpx;
  color: #98a2b3;
  font-size: 23rpx;
  line-height: 1.4;
  text-align: center;
}

/* Premium generated project result */
.result-page {
  min-height: 100vh;
  padding: 18rpx 24rpx 0;
  box-sizing: border-box;
  background:
    radial-gradient(circle at 12% 0%, rgba(109, 40, 217, 0.1), transparent 34%),
    linear-gradient(180deg, #f8faff 0%, #f6f8fc 54%, #ffffff 100%);
}

.result-header {
  position: relative;
  margin: 0 0 18rpx;
  padding: 30rpx;
  border-radius: 32rpx;
  overflow: hidden;
  background:
    radial-gradient(circle at 100% 0%, rgba(109, 40, 217, 0.14), transparent 34%),
    linear-gradient(135deg, #ffffff 0%, #f9fbff 100%);
  border: 1rpx solid rgba(109, 40, 217, 0.1);
  box-shadow: 0 18rpx 42rpx rgba(15, 23, 42, 0.08);
}

.result-title-row {
  margin-bottom: 20rpx;
  gap: 18rpx;
}

.result-project-title {
  color: #101828;
  font-size: 38rpx;
  line-height: 1.26;
  font-weight: 950;
  letter-spacing: 0;
}

.result-duration {
  padding: 8rpx 16rpx;
  border-radius: 999rpx;
  background: #f5f3ff;
  border: 1rpx solid rgba(109, 40, 217, 0.12);
}

.duration-txt {
  color: #6d28d9;
  font-size: 22rpx;
  font-weight: 900;
}

.result-track-row {
  align-items: center;
}

.result-track-badge {
  min-height: 58rpx;
  padding: 0 18rpx;
  border-radius: 999rpx;
  background: #f8fafc;
  border: 1rpx solid #e6ebf2;
}

.result-track-icon {
  font-size: 28rpx;
}

.result-track-label {
  font-size: 24rpx;
  font-weight: 900;
}

.top-btn {
  min-height: 58rpx;
  padding: 0 18rpx;
  border-radius: 999rpx;
  color: #344054;
  background: #ffffff;
  border: 1rpx solid #e6ebf2;
  box-shadow: 0 8rpx 18rpx rgba(15, 23, 42, 0.04);
}

.top-btn-icon,
.top-btn-txt {
  color: #344054;
}

.top-btn-txt {
  font-size: 23rpx;
  font-weight: 850;
}

.result-card {
  margin: 18rpx 0 0;
  padding: 28rpx;
  border-radius: 30rpx;
  background: #ffffff;
  border: 1rpx solid rgba(230, 235, 242, 0.96);
  box-shadow: 0 14rpx 36rpx rgba(15, 23, 42, 0.055);
}

.card-label-row {
  margin-bottom: 18rpx;
  gap: 12rpx;
}

.card-label-icon {
  width: 48rpx;
  height: 48rpx;
  border-radius: 16rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #f5f3ff;
  font-size: 28rpx;
}

.card-label-txt {
  color: #101828;
  font-size: 30rpx;
  line-height: 1.25;
  font-weight: 950;
}

.card-label-sub {
  padding: 6rpx 12rpx;
  border-radius: 999rpx;
  color: #475467;
  background: #f8fafc;
  font-size: 21rpx;
  font-weight: 750;
}

.card-body-text {
  color: #344054;
  font-size: 28rpx;
  line-height: 1.72;
}

.two-col-card {
  gap: 24rpx;
}

.half-col {
  min-width: 0;
}

.col-divider {
  margin: 6rpx 0;
  background: #e6ebf2;
}

.tag-list {
  gap: 10rpx;
}

.data-tag,
.tech-tag {
  max-width: 100%;
  padding: 10rpx 14rpx;
  border-radius: 14rpx;
  font-size: 24rpx;
  line-height: 1.3;
  box-sizing: border-box;
}

.data-tag {
  color: #175cd3;
  background: #eff6ff;
  border-color: #dbeafe;
}

.tech-tag {
  color: #047857;
  background: #ecfdf5;
  border-color: #d1fae5;
}

.results-list {
  gap: 16rpx;
}

.result-item {
  padding: 18rpx;
  border-radius: 20rpx;
  background: #f8fafc;
  border: 1rpx solid #eef2f7;
}

.result-num-badge {
  width: 42rpx;
  height: 42rpx;
  border-radius: 16rpx;
  background: linear-gradient(135deg, #6d28d9, #2563eb);
}

.result-item-txt {
  color: #344054;
  font-size: 27rpx;
  line-height: 1.62;
}

.resume-bullet-card {
  border-color: rgba(109, 40, 217, 0.13);
  background:
    linear-gradient(180deg, #ffffff 0%, #fbfaff 100%);
}

.bullet-copy-hint {
  padding: 8rpx 14rpx;
  border-radius: 999rpx;
  color: #6d28d9;
  background: #f5f3ff;
  font-size: 22rpx;
  font-weight: 850;
}

.resume-bullet-text {
  padding: 20rpx;
  border-left: 0;
  border-radius: 20rpx;
  color: #1d2939;
  background: #f5f7ff;
  font-size: 28rpx;
  line-height: 1.58;
  font-weight: 700;
}

.action-group {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 20;
  margin: 0;
  padding: 18rpx 24rpx calc(18rpx + env(safe-area-inset-bottom));
  gap: 16rpx;
  display: flex;
  background: rgba(255, 255, 255, 0.96);
  border-top: 1rpx solid rgba(230, 235, 242, 0.96);
  box-shadow: 0 -16rpx 34rpx rgba(15, 23, 42, 0.08);
  box-sizing: border-box;
}

.action-btn {
  height: 86rpx;
  border-radius: 26rpx;
  font-size: 29rpx;
  font-weight: 950;
}

.action-copy {
  color: #344054;
  background: #f8fafc;
  border: 1rpx solid #e6ebf2;
}

.action-resume {
  color: #ffffff;
  background: linear-gradient(135deg, #6d28d9, #2563eb);
  box-shadow: 0 14rpx 26rpx rgba(109, 40, 217, 0.22);
}

.action-btn-icon {
  font-size: 28rpx;
}

.action-btn-txt {
  font-size: 29rpx;
  font-weight: 950;
}

.result-page .bottom-spacer {
  height: calc(154rpx + env(safe-area-inset-bottom));
}
