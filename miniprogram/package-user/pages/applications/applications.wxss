/* pages/applications/applications.wxss — White-based premium redesign */
page {
  --bg-page: #f6f8fc;
  --bg-panel: #ffffff;
  --text-primary: #101828;
  --text-body: #344054;
  --text-secondary: #667085;
  --text-muted: #98a2b3;
  --brand: #2563eb;
  --warning: #f59e0b;
  --border: rgba(16,24,40,0.08);
  --border-light: #eaecf0;
  --border-faint: #f1f5f9;
  --r-lg: 28rpx;
  --shadow-sm: 0 12rpx 34rpx rgba(16,24,40,0.06);
  --success-text: #059669;
  --success-bg-solid: #ecfdf3;
  --trans-fast: 0.16s ease;
  background: var(--bg-page);
  font-family: -apple-system, 'PingFang SC', sans-serif;
  -webkit-font-smoothing: antialiased;
}

.container {
  min-height: 100vh;
}

/* ─── Hero — clean white, NO gradient ───────────────────────── */
.hero {
  background: var(--bg-panel);
  padding: 40rpx 28rpx 0;
  border-bottom: 1.5rpx solid rgba(0, 0, 0, 0.06);
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.05);
}

.hero-inner {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 28rpx;
}

.hero-left {
  display: flex;
  flex-direction: column;
}

.hero-title {
  display: block;
  font-size: 40rpx;
  font-weight: 800;
  color: var(--text-primary);
  margin-bottom: 6rpx;
}

.hero-sub {
  font-size: 24rpx;
  color: var(--text-muted);
}

.hero-icon-wrap {
  width: 80rpx;
  height: 80rpx;
  background: #EEF2FF;
  border-radius: var(--r-lg);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.hero-icon {
  font-size: 40rpx;
  line-height: 1;
}

/* Stats bar — clean pill row, no frosted glass */
.hero-stats {
  display: flex;
  align-items: center;
  justify-content: space-evenly;
  padding: 24rpx 0 28rpx;
}

.stat-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6rpx;
}

.stat-num {
  font-size: 40rpx;
  font-weight: 800;
  color: var(--text-primary);
  line-height: 1;
}

.stat-num.orange { color: #F59E0B; }
.stat-num.blue   { color: var(--brand); }
.stat-num.green  { color: #059669; }
.stat-num.gray   { color: var(--text-muted); }

.stat-label {
  font-size: 22rpx;
  color: var(--text-muted);
}

.stat-divider {
  width: 1.5rpx;
  height: 40rpx;
  background: var(--border-faint);
}

/* ─── Hero 进度条 + 转化率 ────────────────────────────────────── */
.hero-bar-wrap {
  padding: 0 0 20rpx;
}
.hero-rates {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
  margin-top: 12rpx;
}
.hero-rate-txt { font-size: 22rpx; color: var(--text-muted); }
.hero-rate-sep { font-size: 22rpx; color: #D1D5DB; }
.hero-rate-val { font-weight: 700; }
.hero-rate-val.blue  { color: var(--brand); }
.hero-rate-val.green { color: #059669; }

/* ─── Pipeline Dashboard ──────────────────────────────────────── */
.pipeline-dash {
  background: var(--bg-panel);
  margin: 0;
  padding: 24rpx 28rpx 20rpx;
  border-bottom: 1.5rpx solid var(--border-faint);
}

.pd-stages {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  margin-bottom: 20rpx;
}

.pd-stage-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6rpx;
  flex: 1;
}

.pd-arrow {
  font-size: 32rpx;
  color: #D1D5DB;
  flex-shrink: 0;
  margin: 0 4rpx;
  padding-bottom: 24rpx;
}

.pd-num {
  font-size: 44rpx;
  font-weight: 800;
  line-height: 1;
}

.pd-pending   { color: #F59E0B; }
.pd-interview { color: var(--brand); }
.pd-offer     { color: #059669; }
.pd-rejected  { color: var(--text-muted); }

.pd-stage-label {
  font-size: 20rpx;
  color: var(--text-muted);
  font-weight: 500;
}

.pd-bar-track {
  display: flex;
  height: 8rpx;
  border-radius: 4rpx;
  overflow: hidden;
  margin-bottom: 16rpx;
  background: var(--border-faint);
}

.pd-bar-seg {
  min-width: 0;
  transition: flex 0.3s;
}

.pd-bar-pending   { background: #FCD34D; }
.pd-bar-interview { background: var(--brand); }
.pd-bar-offer     { background: #059669; }
.pd-bar-rejected  { background: #D1D5DB; }

.pd-rates {
  display: flex;
  align-items: center;
  justify-content: center;
}

.pd-rate-txt {
  font-size: 22rpx;
  color: var(--text-muted);
}

.pd-rate-dot {
  font-size: 22rpx;
  color: #D1D5DB;
  margin: 0 8rpx;
}

.pd-rate-val {
  font-weight: 700;
}

.pd-rate-val.blue  { color: var(--brand); }
.pd-rate-val.green { color: #059669; }

/* ─── Search + Sort ───────────────────────────────────────────── */
.search-sort-row {
  display: flex;
  align-items: center;
  gap: 16rpx;
  background: var(--bg-panel);
  padding: 16rpx 24rpx;
  border-bottom: 1.5rpx solid var(--border-faint);
}
.search-wrap {
  flex: 1;
  display: flex;
  align-items: center;
  background: var(--bg-page);
  border-radius: 40rpx;
  padding: 0 20rpx;
  height: 72rpx;
  gap: 10rpx;
}
.search-icon-txt { font-size: 26rpx; color: var(--text-muted); flex-shrink: 0; }
.search-input {
  flex: 1;
  font-size: 26rpx;
  color: var(--text-primary);
  height: 72rpx;
}
.search-clear {
  font-size: 24rpx;
  color: var(--text-muted);
  flex-shrink: 0;
  padding: 4rpx 8rpx;
}
.sort-btn {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4rpx;
  flex-shrink: 0;
  background: #EEF2FF;
  border-radius: 14rpx;
  padding: 10rpx 16rpx;
}
.sort-icon { font-size: 26rpx; line-height: 1; }
.sort-txt { font-size: 18rpx; color: var(--brand); font-weight: 600; white-space: nowrap; }

/* ─── Filter Tabs ─────────────────────────────────────────────── */
.filter-row {
  display: flex;
  background: var(--bg-panel);
  padding: 0 24rpx;
  border-bottom: 1.5rpx solid var(--border-faint);
  overflow-x: auto;
  position: sticky;
  top: 0;
  z-index: 50;
}

.filter-tab {
  padding: 24rpx 20rpx;
  font-size: 26rpx;
  color: var(--text-muted);
  white-space: nowrap;
  position: relative;
  flex-shrink: 0;
  font-weight: 500;
}

.filter-tab.active {
  color: var(--brand);
  font-weight: 700;
}

.filter-tab.active::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 20rpx;
  right: 20rpx;
  height: 4rpx;
  background: var(--brand);
  border-radius: 2rpx 2rpx 0 0;
}

/* ─── List Area ───────────────────────────────────────────────── */
.list-area {
  padding: 20rpx 24rpx calc(260rpx + env(safe-area-inset-bottom));
}

/* ─── Application Card ────────────────────────────────────────── */
.app-card {
  background: var(--bg-panel);
  border-radius: var(--r-lg);
  margin-bottom: 18rpx;
  overflow: hidden;
  box-shadow: var(--shadow-sm);
  border: 1.5rpx solid var(--border);
  display: flex;
  flex-direction: column;
  position: relative;
  transition: transform var(--trans-fast), opacity var(--trans-fast);
}

.app-card:active {
  transform: scale(0.984);
  opacity: 0.88;
}

/* Left accent bar */
.accent-bar {
  position: absolute;
  left: 0;
  top: 0;
  bottom: 0;
  width: 5rpx;
}

.accent-bar.accent-offer     { background: var(--success-text); }
.accent-bar.accent-interview { background: var(--brand); }
.accent-bar.accent-pending   { background: #F59E0B; }
.accent-bar.accent-rejected  { background: var(--text-muted); }

/* Card body */
.card-body {
  display: flex;
  align-items: center;
  padding: 24rpx 24rpx 20rpx 30rpx;
  gap: 18rpx;
}

/* Company avatar */
.company-avatar {
  width: 80rpx;
  height: 80rpx;
  border-radius: 18rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.avatar-text {
  font-size: 32rpx;
  font-weight: 800;
  color: var(--bg-panel);
}

/* Info column */
.info-col {
  flex: 1;
  min-width: 0;
}

.job-title {
  display: block;
  font-size: 28rpx;
  font-weight: 700;
  color: var(--text-primary);
  margin-bottom: 4rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.company-name {
  display: block;
  font-size: 24rpx;
  color: var(--text-secondary);
  margin-bottom: 10rpx;
}

.meta-row {
  display: flex;
  gap: 10rpx;
  flex-wrap: wrap;
}

.meta-chip {
  font-size: 20rpx;
  color: var(--text-secondary);
  background: var(--bg-page);
  padding: 4rpx 12rpx;
  border-radius: 8rpx;
}

.salary-chip {
  color: var(--success-text);
  background: var(--success-bg-solid);
}

/* Right column */
.right-col {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 10rpx;
  flex-shrink: 0;
}

/* Status badges */
.status-badge {
  font-size: 20rpx;
  font-weight: 600;
  padding: 5rpx 14rpx;
  border-radius: var(--r-lg);
}

.status-badge.offer     { color: var(--success-text); background: var(--success-bg-solid); border: 1rpx solid var(--success-bg); }
.status-badge.interview { color: var(--brand); background: var(--brand-bg); border: 1rpx solid var(--info-bg); }
.status-badge.pending   { color: var(--warning); background: var(--warning-bg); border: 1rpx solid var(--warning-bg); }
.status-badge.rejected  { color: var(--text-muted); background: var(--bg-page); border: 1rpx solid rgba(156,163,175,0.2); }

.date-text {
  font-size: 20rpx;
  color: var(--text-muted);
}

/* ─── Pipeline ────────────────────────────────────────────────── */
.pipeline {
  display: flex;
  align-items: center;
  padding: 16rpx 28rpx 20rpx;
  border-top: 1.5rpx solid var(--border-faint);
}

.p-stage {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6rpx;
}

.p-dot {
  width: 16rpx;
  height: 16rpx;
  border-radius: 50%;
  background: var(--border-light);
  border: 2rpx solid #D1D5DB;
}

.p-stage.reached .p-dot {
  background: var(--brand);
  border-color: var(--brand);
}

.p-label {
  font-size: 18rpx;
  color: #D1D5DB;
  white-space: nowrap;
}

.p-stage.reached .p-label {
  color: var(--brand);
  font-weight: 600;
}

.p-line {
  flex: 1;
  height: 2rpx;
  background: var(--border-light);
  margin: 0 6rpx;
  margin-bottom: 20rpx;
}

.p-line.active {
  background: var(--brand);
}

/* ─── Note Bar ────────────────────────────────────────────────── */
.note-bar {
  display: flex;
  align-items: center;
  padding: 14rpx 28rpx;
  border-top: 1.5rpx solid var(--border-faint);
  gap: 10rpx;
  background: #FAFBFF;
}
.note-bar-icon { font-size: 24rpx; flex-shrink: 0; }
.note-bar-txt {
  flex: 1;
  font-size: 22rpx;
  color: var(--text-muted);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.note-bar-txt:not(:empty) { color: var(--text-secondary); }
.note-bar-edit {
  font-size: 22rpx;
  color: var(--brand);
  flex-shrink: 0;
}

/* ─── Footer / Empty ──────────────────────────────────────────── */
.footer-tip {
  text-align: center;
  color: var(--text-muted);
  font-size: 22rpx;
  margin: 16rpx 0 32rpx;
}


/* ─── FAB ─────────────────────────────────────────────────────── */
.fab {
  position: fixed;
  right: 40rpx;
  bottom: 140rpx;
  width: 100rpx;
  height: 100rpx;
  border-radius: 50%;
  background: var(--brand);
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 8rpx 28rpx rgba(43, 92, 230, 0.4);
  z-index: 50;
  transition: transform 0.12s;
}

.fab:active {
  transform: scale(0.93);
}

.fab-plus {
  font-size: 48rpx;
  color: var(--bg-panel);
  font-weight: 300;
  line-height: 1;
}

/* ─── Modal ───────────────────────────────────────────────────── */
.modal-mask {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.45);
  display: flex;
  align-items: flex-end;
  z-index: 100;
}

.modal-card {
  background: var(--bg-panel);
  border-radius: 32rpx 32rpx 0 0;
  padding: 0 28rpx;
  padding-bottom: calc(32rpx + env(safe-area-inset-bottom));
  width: 100%;
  max-height: 90vh;
  overflow-y: auto;
  box-sizing: border-box;
}

.modal-handle {
  width: 64rpx;
  height: 8rpx;
  background: var(--border-light);
  border-radius: 4rpx;
  margin: 16rpx auto 0;
}

.modal-title {
  display: block;
  font-size: 32rpx;
  font-weight: 700;
  color: var(--text-primary);
  margin: 20rpx 0 24rpx;
  text-align: center;
}

/* 两列布局 */
.form-row {
  display: flex;
  gap: 16rpx;
}
.form-item {
  margin-bottom: 20rpx;
}
.form-item.half { flex: 1; min-width: 0; }

.form-label {
  display: block;
  font-size: 24rpx;
  color: var(--text-body);
  font-weight: 500;
  margin-bottom: 10rpx;
}
.form-required { color: #EF4444; }

.form-input {
  background: #F9FAFB;
  border: 1.5rpx solid var(--border-light);
  border-radius: 14rpx;
  padding: 0 20rpx;
  height: 80rpx;
  font-size: 27rpx;
  color: var(--text-primary);
  width: 100%;
  box-sizing: border-box;
  display: flex;
  align-items: center;
}

/* Picker */
.picker-input {
  display: flex;
  align-items: center;
  gap: 12rpx;
  height: 80rpx;
}
.picker-icon { font-size: 26rpx; flex-shrink: 0; }
.picker-val { color: var(--text-primary); font-size: 27rpx; flex: 1; }
.picker-placeholder { color: #C4C9D4; font-size: 27rpx; flex: 1; }
.picker-clear {
  font-size: 24rpx; color: var(--text-muted);
  padding: 4rpx 10rpx; flex-shrink: 0;
}

/* 状态网格 */
.status-grid {
  display: flex;
  gap: 12rpx;
}
.sg-item {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
  padding: 16rpx 0;
  border-radius: 14rpx;
  background: #F9FAFB;
  border: 1.5rpx solid var(--border-light);
}
.sg-item.sg-active { border-width: 2rpx; }
.sg-item.sg-pending   { background: #FFFBEB; border-color: #F59E0B; }
.sg-item.sg-interview { background: #EEF2FF; border-color: var(--brand); }
.sg-item.sg-offer     { background: #ECFDF5; border-color: #059669; }
.sg-item.sg-rejected  { background: var(--bg-page); border-color: var(--text-muted); }

.sg-dot {
  width: 18rpx; height: 18rpx; border-radius: 50%;
}
.dot-pending   { background: #F59E0B; }
.dot-interview { background: var(--brand); }
.dot-offer     { background: #059669; }
.dot-rejected  { background: var(--text-muted); }

.sg-txt { font-size: 22rpx; font-weight: 500; color: var(--text-body); }
.sg-item.sg-pending   .sg-txt { color: #D97706; }
.sg-item.sg-interview .sg-txt { color: var(--brand); }
.sg-item.sg-offer     .sg-txt { color: #059669; }
.sg-item.sg-rejected  .sg-txt { color: var(--text-secondary); }

.modal-btns {
  display: flex;
  gap: 16rpx;
  margin-top: 28rpx;
}

.modal-btn-cancel { flex: 1; }
.modal-btn-save   { flex: 2; }

/* ─── Loading ─────────────────────────────────────────────────── */
.loading-state {
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 80rpx 40rpx;
  color: var(--text-muted);
  font-size: 26rpx;
  gap: 16rpx;
}

.spinner {
  width: 36rpx;
  height: 36rpx;
  border: 4rpx solid var(--border-light);
  border-top-color: var(--brand);
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

/* ─── Notes ──────────────────────────────────────────────────── */
.notes-preview {
  display: block;
  font-size: 22rpx;
  color: var(--text-muted);
  margin-top: 8rpx;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.deadline-row {
  display: flex;
  align-items: center;
  margin-top: 6rpx;
}

.deadline-text {
  font-size: 22rpx;
  color: var(--text-secondary);
}

.deadline-row.urgent .deadline-text {
  color: #EF4444;
  font-weight: 600;
}

.form-textarea {
  background: var(--bg-page);
  border: 1.5rpx solid rgba(0, 0, 0, 0.07);
  border-radius: 14rpx;
  padding: 22rpx 24rpx;
  font-size: 28rpx;
  color: var(--text-primary);
  width: 100%;
  min-height: 120rpx;
  box-sizing: border-box;
  line-height: 1.6;
}

.note-textarea {
  min-height: 180rpx;
}

/* ── Offer 面板 ───────────────────────────────────────────────── */
.offer-panel {
  display: flex; align-items: center; justify-content: space-between;
  padding: 12rpx 28rpx;
  background: linear-gradient(90deg, #F0FDF4, #ECFDF5);
  border-top: 1rpx solid #D1FAE5;
}
.offer-panel-inner { display: flex; gap: 12rpx; flex-wrap: wrap; }
.offer-chip {
  font-size: 20rpx; font-weight: 600;
  padding: 4rpx 14rpx; border-radius: 9999rpx;
}
.offer-base   { color: #065F46; background: #A7F3D0; }
.offer-bonus  { color: #92400E; background: #FDE68A; }
.offer-rsu    { color: #1E40AF; background: #BFDBFE; }
.offer-edit-hint { font-size: 20rpx; color: #059669; flex-shrink: 0; }

.offer-empty-bar {
  display: flex; align-items: center; gap: 10rpx;
  padding: 12rpx 28rpx;
  background: #F0FDF4; border-top: 1rpx solid #D1FAE5;
}
.offer-empty-icon { font-size: 24rpx; flex-shrink: 0; }
.offer-empty-txt  { font-size: 22rpx; color: #059669; }

/* ── Offer 对比横幅 ───────────────────────────────────────────── */
.offer-compare-banner {
  display: flex; align-items: center; justify-content: space-between;
  margin: 16rpx 24rpx 0;
  background: linear-gradient(135deg, #065F46, #059669);
  border-radius: var(--r-lg);
  padding: 20rpx 24rpx;
  gap: 16rpx;
}
.offer-compare-banner:active { opacity: 0.85; }
.ocb-left { display: flex; align-items: center; gap: 16rpx; flex: 1; }
.ocb-icon { font-size: 36rpx; flex-shrink: 0; }
.ocb-info { flex: 1; }
.ocb-title { display: block; font-size: 27rpx; font-weight: 700; color: #fff; margin-bottom: 4rpx; }
.ocb-sub   { display: block; font-size: 22rpx; color: rgba(255,255,255,0.8); }
.ocb-arrow { font-size: 36rpx; color: rgba(255,255,255,0.7); flex-shrink: 0; }

/* ── VIP 优先投递标识 ─────────────────────────────────────────── */
.vip-priority-badge {
  font-size: 18rpx;
  font-weight: 700;
  color: #92400e;
  background: linear-gradient(135deg, #fef9c3, #fde68a);
  border: 1rpx solid #f59e0b;
  padding: 4rpx 10rpx;
  border-radius: 14rpx;
  white-space: nowrap;
  text-align: center;
  margin-bottom: 8rpx;
  align-self: flex-end;
}

/* 2026 polish: compact tracker UI and richer tap feedback */
page {
  background:
    radial-gradient(circle at 14% 0%, rgba(44, 111, 255, 0.12), transparent 34%),
    linear-gradient(180deg, #f7faff 0%, #f5f7fb 48%, #ffffff 100%);
  overflow-x: hidden;
}

.container {
  overflow-x: hidden;
  padding-bottom: env(safe-area-inset-bottom);
}

.hero {
  position: relative;
  margin: 14rpx 24rpx 0;
  padding: 34rpx 24rpx 22rpx;
  border: 0;
  border-radius: 32rpx;
  overflow: hidden;
  background: linear-gradient(135deg, #0e2569 0%, #2f66f4 58%, #19c8b0 100%);
  box-shadow: 0 22rpx 42rpx rgba(32, 94, 226, 0.22);
}

.hero-orb {
  position: absolute;
  border-radius: 999rpx;
  pointer-events: none;
}

.hero-orb-a {
  width: 260rpx;
  height: 260rpx;
  right: -92rpx;
  top: -92rpx;
  background: rgba(255, 255, 255, 0.16);
}

.hero-orb-b {
  width: 150rpx;
  height: 150rpx;
  left: -56rpx;
  bottom: -54rpx;
  background: rgba(0, 255, 209, 0.18);
}

.hero-inner,
.hero-stats,
.hero-bar-wrap {
  position: relative;
  z-index: 1;
}

.hero-inner {
  margin-bottom: 22rpx;
}

.hero-title {
  color: #ffffff;
  font-size: 42rpx;
  letter-spacing: -0.8rpx;
}

.hero-sub {
  color: rgba(255, 255, 255, 0.76);
  font-size: 25rpx;
}

.hero-icon-wrap {
  width: 84rpx;
  height: 84rpx;
  border-radius: 26rpx;
  background: rgba(255, 255, 255, 0.16);
  border: 1rpx solid rgba(255, 255, 255, 0.28);
  box-shadow: inset 0 1rpx 0 rgba(255, 255, 255, 0.24);
}

.hero-stats {
  margin: 0;
  padding: 20rpx 10rpx;
  border-radius: 26rpx;
  background: rgba(255, 255, 255, 0.14);
  border: 1rpx solid rgba(255, 255, 255, 0.2);
  backdrop-filter: blur(18rpx);
}

.stat-item {
  min-width: 118rpx;
  border-radius: 20rpx;
}

.stat-num {
  color: #ffffff;
  font-size: 38rpx;
}

.stat-num.orange { color: #ffd166; }
.stat-num.blue { color: #c9dcff; }
.stat-num.green { color: #b7f7dc; }

.stat-label,
.hero-rate-txt,
.hero-rate-sep {
  color: rgba(255, 255, 255, 0.72);
}

.stat-divider {
  background: rgba(255, 255, 255, 0.22);
}

.hero-bar-wrap {
  padding: 16rpx 2rpx 0;
}

.hero .pd-bar-track {
  height: 10rpx;
  margin-bottom: 14rpx;
  background: rgba(255, 255, 255, 0.22);
}

.hero-rate-val.blue {
  color: #d9e6ff;
}

.hero-rate-val.green {
  color: #b9f6d7;
}

.search-sort-row {
  gap: 14rpx;
  margin: 22rpx 24rpx 0;
  padding: 0;
  background: transparent;
  border: 0;
}

.search-wrap {
  height: 76rpx;
  padding: 0 24rpx;
  border-radius: 26rpx;
  background: rgba(255, 255, 255, 0.94);
  border: 1rpx solid rgba(33, 73, 151, 0.08);
  box-shadow: 0 12rpx 28rpx rgba(20, 38, 82, 0.07);
}

.search-input {
  height: 76rpx;
  font-weight: 500;
}

.sort-btn {
  width: 96rpx;
  min-height: 76rpx;
  padding: 8rpx 10rpx;
  border-radius: 24rpx;
  background: linear-gradient(180deg, #ffffff 0%, #eef4ff 100%);
  border: 1rpx solid rgba(47, 102, 244, 0.12);
  box-shadow: 0 12rpx 24rpx rgba(47, 102, 244, 0.11);
}

.sort-txt {
  font-size: 19rpx;
  color: #2f66f4;
}

.filter-row {
  margin: 18rpx 24rpx 0;
  padding: 8rpx;
  border: 0;
  border-radius: 26rpx;
  background: rgba(255, 255, 255, 0.92);
  box-shadow: 0 12rpx 28rpx rgba(20, 38, 82, 0.06);
  overflow: hidden;
  top: 0;
}

.filter-tab {
  flex: 1;
  padding: 16rpx 10rpx;
  border-radius: 18rpx;
  text-align: center;
  font-size: 24rpx;
  color: #8d98aa;
}

.filter-tab.active {
  color: #ffffff;
  background: linear-gradient(135deg, #2f66f4, #16b8a7);
  box-shadow: 0 10rpx 22rpx rgba(47, 102, 244, 0.22);
}

.filter-tab.active::after {
  display: none;
}

.list-area {
  padding: 22rpx 24rpx calc(78rpx + env(safe-area-inset-bottom));
}

.list-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin: 0 4rpx 18rpx;
}

.list-head-left {
  display: flex;
  flex-direction: column;
  gap: 4rpx;
}

.list-kicker {
  font-size: 18rpx;
  font-weight: 800;
  color: #8aa0c6;
  letter-spacing: 2rpx;
}

.list-title {
  font-size: 30rpx;
  font-weight: 800;
  color: #15213a;
}

.list-count {
  padding: 8rpx 16rpx;
  border-radius: 999rpx;
  color: #4771d9;
  background: #eef4ff;
  font-size: 22rpx;
  font-weight: 700;
}

.app-card {
  margin-bottom: 22rpx;
  border: 1rpx solid rgba(33, 73, 151, 0.07);
  border-radius: 30rpx;
  background: linear-gradient(180deg, #ffffff 0%, #fbfdff 100%);
  box-shadow: 0 18rpx 40rpx rgba(22, 35, 66, 0.08);
  animation: appCardIn 0.34s ease both;
}

.accent-bar {
  width: 8rpx;
  border-radius: 999rpx;
  top: 22rpx;
  bottom: 22rpx;
  left: 16rpx;
}

.card-body {
  align-items: flex-start;
  padding: 28rpx 22rpx 20rpx 38rpx;
  gap: 18rpx;
}

.company-avatar {
  width: 88rpx;
  height: 88rpx;
  border-radius: 24rpx;
  box-shadow: 0 12rpx 24rpx rgba(34, 73, 135, 0.16);
}

.avatar-text {
  font-size: 34rpx;
}

.job-title {
  font-size: 30rpx;
  letter-spacing: -0.4rpx;
  margin-bottom: 8rpx;
}

.company-name {
  color: #6b778c;
  font-weight: 600;
  margin-bottom: 12rpx;
}

.meta-chip {
  padding: 6rpx 14rpx;
  border-radius: 999rpx;
  background: #f2f6ff;
  color: #66738c;
  font-weight: 600;
}

.salary-chip {
  color: #07906d;
  background: #e5fbf2;
}

.right-col {
  min-width: 108rpx;
}

.status-badge {
  padding: 7rpx 16rpx;
  border-radius: 999rpx;
  font-size: 21rpx;
  box-shadow: 0 6rpx 14rpx rgba(20, 38, 82, 0.06);
}

.date-text {
  color: #99a4b5;
}

.pipeline {
  margin: 0 28rpx 0 38rpx;
  padding: 18rpx 0 20rpx;
  border-top: 1rpx dashed rgba(97, 117, 154, 0.18);
}

.p-dot {
  width: 18rpx;
  height: 18rpx;
  border: 4rpx solid #edf1f7;
  background: #cfd7e3;
}

.p-stage.reached .p-dot {
  background: #ffffff;
  border-color: #2f66f4;
  box-shadow: 0 0 0 5rpx rgba(47, 102, 244, 0.12);
}

.p-line {
  height: 4rpx;
  border-radius: 999rpx;
  background: #edf1f7;
}

.p-line.active {
  background: linear-gradient(90deg, #2f66f4, #16b8a7);
}

.p-label {
  font-size: 19rpx;
  color: #a6b0bf;
}

.p-stage.reached .p-label {
  color: #2f66f4;
}

.notes-preview {
  max-width: 350rpx;
  margin-top: 10rpx;
  color: #8792a5;
}

.deadline-row {
  display: inline-flex;
  margin-top: 10rpx;
  padding: 5rpx 12rpx;
  border-radius: 999rpx;
  background: #fff7ed;
}

.deadline-text {
  color: #c46a17;
  font-size: 20rpx;
}

.note-bar {
  margin: 0 18rpx 18rpx 38rpx;
  padding: 14rpx 18rpx;
  border: 0;
  border-radius: 18rpx;
  background: #f7faff;
}

.note-bar-icon {
  width: 34rpx;
  height: 34rpx;
  border-radius: 10rpx;
  text-align: center;
  line-height: 34rpx;
  background: #ffffff;
}

.note-bar-edit {
  font-weight: 700;
}

.offer-panel,
.offer-empty-bar {
  margin: 0 18rpx 16rpx 38rpx;
  border-radius: 18rpx;
  border-top: 0;
}

.offer-compare-banner {
  margin: 18rpx 24rpx 0;
  border-radius: 28rpx;
  box-shadow: 0 16rpx 34rpx rgba(5, 150, 105, 0.18);
}

.fab {
  right: 34rpx;
  bottom: calc(58rpx + env(safe-area-inset-bottom));
  width: 108rpx;
  height: 108rpx;
  background: linear-gradient(135deg, #2f66f4 0%, #16b8a7 100%);
  box-shadow: 0 18rpx 36rpx rgba(47, 102, 244, 0.36);
}

.footer-tip {
  margin: 4rpx 0 12rpx;
}

.fab::after {
  content: '';
  position: absolute;
  inset: -10rpx;
  border-radius: 999rpx;
  border: 1rpx solid rgba(47, 102, 244, 0.18);
}

.fab-plus {
  font-weight: 400;
}

.modal-mask {
  background: rgba(9, 18, 38, 0.48);
  backdrop-filter: blur(8rpx);
}

.modal-card {
  border-radius: 36rpx 36rpx 0 0;
  box-shadow: 0 -16rpx 42rpx rgba(9, 18, 38, 0.16);
}

.form-input,
.form-textarea {
  border-radius: 20rpx;
  background: #f7faff;
  border-color: rgba(47, 102, 244, 0.12);
}

.sg-item {
  border-radius: 20rpx;
}

.tap-bounce,
.tab-press,
.chip-press,
.soft-press {
  transform: scale(0.96);
  opacity: 0.86;
}

.card-press {
  transform: translateY(4rpx) scale(0.985);
  opacity: 0.92;
}

.fab-press {
  transform: scale(0.9);
  opacity: 0.9;
}

@keyframes appCardIn {
  from {
    opacity: 0;
    transform: translateY(18rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.project-panel {
  margin: 18rpx 24rpx 0;
  padding: 22rpx;
  border-radius: 28rpx;
  background: rgba(255,255,255,0.94);
  border: 1rpx solid rgba(33,73,151,0.08);
  box-shadow: 0 14rpx 32rpx rgba(20,38,82,0.065);
}

.project-panel-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 18rpx;
}

.project-kicker,
.project-title {
  display: block;
}

.project-kicker {
  color: #8aa0c6;
  font-size: 18rpx;
  font-weight: 900;
  letter-spacing: 2rpx;
}

.project-title {
  margin-top: 4rpx;
  color: #15213a;
  font-size: 30rpx;
  font-weight: 900;
}

.project-add {
  padding: 10rpx 18rpx;
  border-radius: 999rpx;
  color: #ffffff;
  background: linear-gradient(135deg, #2f66f4, #16b8a7);
  font-size: 23rpx;
  font-weight: 900;
}

.project-active-filter {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12rpx;
  margin-bottom: 16rpx;
  padding: 12rpx 16rpx;
  border-radius: 18rpx;
  color: #1d4ed8;
  background: #eef4ff;
  font-size: 23rpx;
  font-weight: 800;
}

.project-active-filter view {
  color: #ef4444;
  flex-shrink: 0;
}

.project-scroll,
.project-choice-scroll {
  width: 100%;
}

.project-track,
.project-choice-row {
  display: inline-flex;
  gap: 14rpx;
  padding-bottom: 4rpx;
}

.project-card {
  width: 300rpx;
  padding: 18rpx;
  border-radius: 22rpx;
  background: #f8fbff;
  border: 1rpx solid #e6ebf2;
  box-sizing: border-box;
}

.project-card.selected {
  border-color: #2f66f4;
  box-shadow: 0 10rpx 24rpx rgba(47,102,244,0.14);
}

.project-top {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 12rpx;
}

.project-name {
  flex: 1;
  color: #15213a;
  font-size: 26rpx;
  font-weight: 900;
  line-height: 1.3;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.project-type {
  flex-shrink: 0;
  max-width: 108rpx;
  padding: 4rpx 10rpx;
  border-radius: 999rpx;
  font-size: 18rpx;
  font-weight: 900;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.project-desc {
  display: block;
  margin-top: 10rpx;
  color: #748198;
  font-size: 22rpx;
  line-height: 1.45;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.project-stats {
  display: flex;
  gap: 8rpx;
  flex-wrap: wrap;
  margin-top: 14rpx;
}

.project-stats text {
  padding: 5rpx 10rpx;
  border-radius: 999rpx;
  color: #4771d9;
  background: #eef4ff;
  font-size: 19rpx;
  font-weight: 800;
}

.project-empty {
  padding: 18rpx;
  border-radius: 22rpx;
  background: #f8fbff;
  border: 1rpx dashed #c7d2fe;
}

.project-empty text {
  display: block;
  color: #667085;
  font-size: 24rpx;
  line-height: 1.5;
}

.project-empty view {
  display: inline-flex;
  margin-top: 14rpx;
  padding: 10rpx 18rpx;
  border-radius: 999rpx;
  color: #2563eb;
  background: #eef4ff;
  font-size: 23rpx;
  font-weight: 900;
}

.project-choice-row {
  gap: 12rpx;
}

.project-choice {
  flex-shrink: 0;
  padding: 13rpx 20rpx;
  border-radius: 999rpx;
  color: #667085;
  background: #f7faff;
  border: 1rpx solid rgba(47,102,244,0.10);
  font-size: 24rpx;
  font-weight: 800;
}

.project-choice.active {
  color: #ffffff;
  background: linear-gradient(135deg, #2f66f4, #16b8a7);
  border-color: transparent;
}

.project-cat-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 12rpx;
}

.project-cat {
  min-height: 70rpx;
  padding: 0 14rpx;
  border-radius: 18rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #667085;
  background: #f7faff;
  border: 1rpx solid rgba(47,102,244,0.10);
  font-size: 24rpx;
  font-weight: 850;
  text-align: center;
}

.project-cat.active {
  border-width: 2rpx;
}
/* Unified homepage background */
page,
.page,
.container {
  --app-page-bg: radial-gradient(circle at 18% 0%, rgba(43, 92, 230, 0.08) 0%, rgba(43, 92, 230, 0) 34%), linear-gradient(180deg, #f8fbff 0%, #ffffff 42%);
  background: var(--app-page-bg);
  background-color: #ffffff;
}

/* 2026 applications refinement: quiet, focused, premium */
page,
.page,
.container {
  --app-page-bg: linear-gradient(180deg, #f7f9fc 0%, #f4f6fa 100%);
  background: var(--app-page-bg);
  background-color: #f5f7fb;
}

.container {
  padding: 18rpx 0 calc(36rpx + env(safe-area-inset-bottom));
}

.hero {
  margin: 0 24rpx;
  padding: 32rpx 28rpx 24rpx;
  border-radius: 32rpx;
  background: linear-gradient(135deg, #132b5f 0%, #1c4aa0 55%, #2f6fed 100%);
  box-shadow: 0 18rpx 40rpx rgba(28, 74, 160, 0.18);
}

.hero-orb-a {
  width: 250rpx;
  height: 250rpx;
  right: -100rpx;
  top: -120rpx;
  background: rgba(255, 255, 255, 0.09);
}

.hero-orb-b {
  width: 140rpx;
  height: 140rpx;
  left: auto;
  right: 78rpx;
  bottom: -92rpx;
  background: rgba(114, 181, 255, 0.12);
}

.hero-inner {
  margin-bottom: 24rpx;
}

.hero-title {
  margin-bottom: 8rpx;
  font-size: 40rpx;
  font-weight: 800;
  letter-spacing: 0;
}

.hero-sub {
  font-size: 23rpx;
  color: rgba(255, 255, 255, 0.68);
}

.hero-icon-wrap {
  width: 76rpx;
  height: 76rpx;
  border-radius: 22rpx;
  background: rgba(255, 255, 255, 0.11);
  border: 1rpx solid rgba(255, 255, 255, 0.18);
  box-shadow: none;
}

.hero-document {
  position: relative;
  width: 32rpx;
  height: 40rpx;
  padding: 9rpx 7rpx 6rpx;
  border: 3rpx solid rgba(255, 255, 255, 0.92);
  border-radius: 7rpx;
  box-sizing: border-box;
}

.document-dot {
  width: 6rpx;
  height: 6rpx;
  margin-bottom: 5rpx;
  border-radius: 50%;
  background: #9fc4ff;
}

.document-line {
  width: 14rpx;
  height: 3rpx;
  margin-bottom: 4rpx;
  border-radius: 99rpx;
  background: rgba(255, 255, 255, 0.72);
}

.document-line-long { width: 18rpx; }
.document-line-short { width: 10rpx; }

.hero-stats {
  padding: 20rpx 4rpx 18rpx;
  border-radius: 22rpx;
  background: rgba(255, 255, 255, 0.09);
  border-color: rgba(255, 255, 255, 0.12);
}

.stat-item { min-width: 112rpx; }

.stat-num,
.stat-num.orange,
.stat-num.blue,
.stat-num.green {
  color: #ffffff;
  font-size: 34rpx;
  font-weight: 750;
}

.stat-label {
  color: rgba(255, 255, 255, 0.58);
  font-size: 20rpx;
}

.stat-divider {
  height: 34rpx;
  background: rgba(255, 255, 255, 0.12);
}

.hero-bar-wrap { padding-top: 14rpx; }
.hero-rates { margin-top: 10rpx; }

.search-sort-row {
  gap: 12rpx;
  margin: 20rpx 24rpx 0;
}

.search-wrap {
  height: 76rpx;
  padding: 0 22rpx;
  border-radius: 22rpx;
  border: 1rpx solid #e8ecf2;
  background: #ffffff;
  box-shadow: 0 8rpx 24rpx rgba(16, 24, 40, 0.05);
}

.search-icon {
  position: relative;
  width: 24rpx;
  height: 24rpx;
  margin-right: 15rpx;
  border: 3rpx solid #7f8ca3;
  border-radius: 50%;
  box-sizing: border-box;
  flex-shrink: 0;
}

.search-icon::after {
  content: '';
  position: absolute;
  width: 9rpx;
  height: 3rpx;
  right: -7rpx;
  bottom: -4rpx;
  border-radius: 99rpx;
  background: #7f8ca3;
  transform: rotate(45deg);
}

.search-input {
  height: 76rpx;
  color: #182230;
  font-size: 25rpx;
  font-weight: 450;
}

.search-clear {
  color: #98a2b3;
  font-size: 22rpx;
}

.sort-btn {
  width: 178rpx;
  min-height: 76rpx;
  padding: 0 17rpx;
  flex-direction: row;
  justify-content: center;
  gap: 8rpx;
  border-radius: 22rpx;
  border: 1rpx solid #e3e9f2;
  background: #ffffff;
  box-shadow: 0 8rpx 24rpx rgba(16, 24, 40, 0.05);
  box-sizing: border-box;
}

.sort-label {
  color: #98a2b3;
  font-size: 19rpx;
}

.sort-txt {
  color: #344054;
  font-size: 22rpx;
  font-weight: 650;
}

.sort-chevron {
  margin-top: -5rpx;
  color: #98a2b3;
  font-size: 22rpx;
}

.filter-row {
  margin: 16rpx 24rpx 0;
  padding: 6rpx;
  border: 1rpx solid #e8ecf2;
  border-radius: 22rpx;
  background: #ffffff;
  box-shadow: 0 6rpx 20rpx rgba(16, 24, 40, 0.035);
}

.filter-tab {
  padding: 14rpx 6rpx;
  border-radius: 16rpx;
  color: #7c8799;
  font-size: 22rpx;
  font-weight: 500;
}

.filter-tab.active {
  color: #1f4fa7;
  background: #edf3ff;
  box-shadow: none;
  font-weight: 700;
}

.project-panel {
  margin: 16rpx 24rpx 0;
  padding: 22rpx 24rpx;
  border: 1rpx solid #e8ecf2;
  border-radius: 26rpx;
  background: #ffffff;
  box-shadow: 0 8rpx 24rpx rgba(16, 24, 40, 0.045);
}

.project-panel-head {
  margin-bottom: 16rpx;
}

.project-title {
  margin: 0;
  color: #172033;
  font-size: 28rpx;
  font-weight: 750;
}

.project-subtitle {
  display: block;
  margin-top: 5rpx;
  color: #98a2b3;
  font-size: 20rpx;
}

.project-add {
  display: flex;
  align-items: center;
  gap: 4rpx;
  padding: 9rpx 15rpx;
  color: #2459b8;
  border: 1rpx solid #d8e4fa;
  background: #f3f7ff;
  box-shadow: none;
  font-size: 21rpx;
  font-weight: 700;
}

.project-add-plus {
  font-size: 23rpx;
  font-weight: 500;
}

.project-empty {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18rpx;
  padding: 17rpx 18rpx;
  border: 1rpx solid #e9eef5;
  border-radius: 18rpx;
  background: #f8fafc;
}

.project-empty .project-empty-copy {
  display: block;
  min-width: 0;
  margin: 0;
  padding: 0;
  border-radius: 0;
  color: inherit;
  background: transparent;
  font-size: inherit;
  font-weight: inherit;
  flex: 1;
}

.project-empty .project-empty-title {
  display: block;
  color: #344054;
  font-size: 23rpx;
  font-weight: 700;
  line-height: 1.3;
}

.project-empty .project-empty-desc {
  display: block;
  margin-top: 5rpx;
  color: #8b95a7;
  font-size: 20rpx;
  line-height: 1.45;
}

.project-empty .project-empty-link {
  display: flex;
  align-items: center;
  flex-shrink: 0;
  margin: 0;
  padding: 8rpx 0 8rpx 12rpx;
  color: #2459b8;
  background: transparent;
  font-size: 21rpx;
  font-weight: 700;
}

.project-empty-link text {
  margin-left: 3rpx;
  font-size: 27rpx;
}

.project-card {
  border-color: #e7ecf3;
  background: #f8fafc;
  box-shadow: none;
}

.project-card.selected {
  border-color: #7aa2eb;
  background: #f4f7ff;
  box-shadow: 0 8rpx 20rpx rgba(36, 89, 184, 0.1);
}

.offer-compare-banner {
  margin: 16rpx 24rpx 0;
  border-radius: 24rpx;
  background: linear-gradient(135deg, #16346f, #2459b8);
  box-shadow: 0 12rpx 30rpx rgba(36, 89, 184, 0.16);
}

.ocb-icon {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 58rpx;
  height: 58rpx;
  border: 1rpx solid rgba(255, 255, 255, 0.22);
  border-radius: 18rpx;
  background: rgba(255, 255, 255, 0.12);
  color: #ffffff;
  font-size: 20rpx;
  font-weight: 800;
  letter-spacing: 1rpx;
}

.list-area {
  padding: 26rpx 24rpx calc(88rpx + env(safe-area-inset-bottom));
}

.list-head {
  margin: 0 2rpx 18rpx;
}

.list-head-left { gap: 4rpx; }

.list-title {
  color: #172033;
  font-size: 29rpx;
  font-weight: 750;
}

.list-subtitle {
  color: #98a2b3;
  font-size: 20rpx;
}

.list-count {
  padding: 7rpx 14rpx;
  color: #667085;
  background: #eef1f5;
  font-size: 20rpx;
  font-weight: 600;
}

.app-card {
  border-color: #e7ebf1;
  border-radius: 26rpx;
  background: #ffffff;
  box-shadow: 0 10rpx 28rpx rgba(16, 24, 40, 0.055);
}

.company-avatar {
  border-radius: 20rpx;
  box-shadow: none;
}

.meta-chip {
  color: #667085;
  background: #f1f4f8;
  font-weight: 550;
}

.salary-chip {
  color: #08785f;
  background: #eaf8f3;
}

.status-badge { box-shadow: none; }

.note-bar-icon {
  color: #667085;
  background: #eef2f7;
  font-size: 18rpx;
  font-weight: 700;
}

.offer-empty-icon {
  padding: 4rpx 8rpx;
  border-radius: 8rpx;
  color: #08785f;
  background: #dff5ec;
  font-size: 15rpx;
  font-weight: 800;
  letter-spacing: 1rpx;
}

.application-empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 50rpx 40rpx 62rpx;
  border: 1rpx solid #e8ecf2;
  border-radius: 28rpx;
  background: #ffffff;
  box-shadow: 0 8rpx 26rpx rgba(16, 24, 40, 0.04);
}

.empty-visual {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 120rpx;
  height: 120rpx;
  margin-bottom: 26rpx;
  border-radius: 36rpx;
  background: linear-gradient(145deg, #eef4ff, #f7f9fd);
}

.empty-sheet {
  width: 50rpx;
  height: 62rpx;
  padding: 14rpx 11rpx;
  border-radius: 11rpx;
  background: #ffffff;
  box-shadow: 0 8rpx 20rpx rgba(36, 89, 184, 0.12);
  box-sizing: border-box;
  transform: rotate(-3deg);
}

.empty-sheet-mark {
  width: 12rpx;
  height: 12rpx;
  margin-bottom: 9rpx;
  border-radius: 4rpx;
  background: #3b73dc;
}

.empty-sheet-line {
  width: 20rpx;
  height: 4rpx;
  margin-bottom: 6rpx;
  border-radius: 99rpx;
  background: #ccd9ef;
}

.empty-sheet-line-long { width: 28rpx; }

.empty-visual-dot {
  position: absolute;
  border-radius: 50%;
}

.empty-visual-dot-a {
  width: 14rpx;
  height: 14rpx;
  right: 19rpx;
  top: 25rpx;
  background: #78a2ed;
}

.empty-visual-dot-b {
  width: 9rpx;
  height: 9rpx;
  left: 21rpx;
  bottom: 27rpx;
  background: #c1d3f4;
}

.empty-title {
  color: #172033;
  font-size: 30rpx;
  font-weight: 750;
}

.empty-desc {
  max-width: 500rpx;
  margin-top: 12rpx;
  color: #7f899a;
  font-size: 23rpx;
  line-height: 1.65;
  text-align: center;
}

.empty-primary {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 6rpx;
  min-width: 276rpx;
  height: 76rpx;
  margin-top: 30rpx;
  padding: 0 28rpx;
  border-radius: 20rpx;
  color: #ffffff;
  background: #2459b8;
  box-shadow: 0 10rpx 24rpx rgba(36, 89, 184, 0.2);
  font-size: 25rpx;
  font-weight: 700;
  box-sizing: border-box;
}

.empty-primary-plus {
  font-size: 28rpx;
  font-weight: 400;
}

.empty-primary-ghost {
  color: #2459b8;
  border: 1rpx solid #cad9f3;
  background: #f3f7ff;
  box-shadow: none;
}

.empty-secondary {
  margin-top: 22rpx;
  padding: 8rpx 14rpx;
  color: #667085;
  font-size: 22rpx;
}

.empty-secondary text {
  margin-left: 3rpx;
  color: #2459b8;
  font-size: 27rpx;
}

.fab {
  right: 32rpx;
  bottom: calc(54rpx + env(safe-area-inset-bottom));
  width: 100rpx;
  height: 100rpx;
  background: #2459b8;
  box-shadow: 0 16rpx 34rpx rgba(36, 89, 184, 0.3);
}

.fab::after {
  inset: -8rpx;
  border-color: rgba(36, 89, 184, 0.14);
}
